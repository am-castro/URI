#include <stdio.h>
#include <conio.h>

int main() {
 
    int N=0,atacked=0,v=1;
    scanf("%d",&N);
    int i[N];

 	for(v=1;v<=N;v++){
 		scanf("%d",&i[v]);
	}
	while(v>0 && v<=N){
		
	}
	
	printf("%d %d",N,atacked);
	
 	getch();
    return 0;
}
